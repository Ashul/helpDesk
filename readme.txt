client
1.create signup view 



server

1.create (CRUD)signup
* initalize the required module
* initalize middlewares
* mongodb setup
* include all model files using fs module
* include all controller files using fs module
* listen to port
